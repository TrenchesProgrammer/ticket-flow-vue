
{
  "buildCommand": "rm -rf node_modules package-lock.json && npm install && npm run build-only",
  "outputDirectory": "dist",
  "framework": "vite",
  "installCommand": "npm install --include=optional"
}